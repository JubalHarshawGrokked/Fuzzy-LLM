am programaze:
from pyswip import Prolog

prolog = Prolog()

# Define simple facts and rules directly
prolog.assertz("good_student(X) :- good_grades(X), polite(X)")
prolog.assertz("good_grades(john)")
prolog.assertz("polite(john)")


# 'program': 'good_student(X) :- good_grades(X), polite(X).\n\ngood_grades(john).\npolite(john).', 'query': 'good_student(john).'

# Run a query
results = list(prolog.query("good_student(john)"))

print("Crisp Prolog result:")
print(results)

RATOA SHEDEGI:
Crisp Prolog result:
[{}]